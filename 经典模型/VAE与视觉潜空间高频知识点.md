# 目录

## 第一章 AE、VAE 与生成潜空间

[1. AE、VAE 与 VQ-VAE 如何构建视觉潜空间？](#q-001)
  - [面试问题：AE、VAE 和 VQ-VAE 的目标分别是什么？](#q-002)
  - [面试问题：自编码器 AE 的基本结构和训练流程是什么？](#q-003)
  - [面试问题：AE 为什么不能直接作为稳定的生成模型？](#q-004)

## 第二章 VAE 的概率建模与重参数化

[2. VAE 如何在连续潜空间中实现生成？](#q-005)
  - [面试问题：VAE 的 ELBO 损失由哪些部分组成？](#q-006)
  - [面试问题：什么是重参数化技巧，为什么 VAE 必须使用它？](#q-007)
  - [面试问题：VAE 生成的图像为什么容易模糊？如何缓解？](#q-008)

## 第三章 VAE 与扩散模型的工程连接

[3. 为什么 Stable Diffusion 等系统需要 VAE？](#q-009)
  - [面试问题：Diffusion Model 和 VAE 中的重参数化分别如何使用？](#q-010)
  - [面试问题：视觉 tokenizer 的重构质量为什么不等于生成质量？](#q-011)

---

<h1 id="q-001">1. AE、VAE 与 VQ-VAE 如何构建视觉潜空间？</h1>

<h2 id="q-002">面试问题：AE、VAE 和 VQ-VAE 的目标分别是什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

三者都通过编码器把高维图像压缩到潜变量，再由解码器重构图像，但潜变量的性质不同。

| 模型 | 潜变量形式 | 训练目标 | 典型用途 |
|---|---|---|---|
| AE | 确定性连续向量 | 重构误差 | 降维、去噪、特征提取 |
| VAE | 参数化概率分布 | 重构误差 + KL 正则 | 连续潜空间生成、插值、异常检测 |
| VQ-VAE | 离散码本索引 | 重构误差 + 码本/承诺损失 | 视觉 tokenizer、离散生成、压缩 |

AE 只要求输入经过编码和解码后尽量还原，潜空间不一定连续，也不保证从任意随机向量都能生成合理图像。VAE 对潜变量分布施加先验约束，使从先验采样成为可能；VQ-VAE 则把连续特征量化到有限码本，便于把图像转成 token 序列交给自回归模型或其他序列模型。

面试中可以这样收束：**AE 解决重构，VAE 解决连续概率生成，VQ-VAE 解决可学习的离散视觉表示；选型取决于系统需要连续 latent、离散 token 还是单纯压缩。**

<h2 id="q-003">面试问题：自编码器 AE 的基本结构和训练流程是什么？</h2>

**难度评分：⭐⭐ (2/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

AE 由 Encoder 和 Decoder 两部分组成。给定输入图像 `x`，编码器得到潜变量 `z=f_phi(x)`，解码器再生成重构结果 `x_hat=g_theta(z)`。训练时最小化输入与重构之间的差异，例如均方误差、L1 损失或感知损失。

```math
z=f_\phi(x),\qquad \hat{x}=g_\theta(z),\qquad
\mathcal{L}_{\mathrm{AE}}=d(x,\hat{x})
```

Encoder 通常逐步下采样并增加通道，提取压缩后的语义和纹理；Decoder 通过上采样、转置卷积或残差块恢复空间分辨率。AE 的关键工程权衡是压缩率：压得太狠会丢失细节，压得太松则无法显著降低后续模型的计算量。

<h2 id="q-004">面试问题：AE 为什么不能直接作为稳定的生成模型？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

AE 学到的是训练样本到潜变量的确定性映射，没有要求所有样本的潜变量服从某个已知先验。潜空间可能出现空洞、不连续和簇间断裂：从标准正态分布随机采样一个向量，再送入 Decoder，往往落在训练分布之外，输出就可能失真。

AE 仍然适合降维、去噪、异常检测和特征提取。若要生成，需要额外学习潜变量分布，或者在潜空间上训练扩散模型、流模型或自回归模型。VAE 的 KL 正则、VQ-VAE 的码本约束，都是把“可重构表示”进一步变成“可采样表示”的手段。

<h1 id="q-005">2. VAE 如何在连续潜空间中实现生成？</h1>

<h2 id="q-006">面试问题：VAE 的 ELBO 损失由哪些部分组成？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

VAE 的 Encoder 不直接输出一个确定向量，而是输出后验分布 `q_phi(z|x)` 的均值和方差参数，再从中采样 `z` 交给 Decoder。训练目标是最大化观测数据对数似然的变分下界，等价于最小化重构损失和 KL 散度：

```math
\mathcal{L}_{\mathrm{VAE}}=
\mathbb{E}_{q_\phi(z\mid x)}[-\log p_\theta(x\mid z)]
+\beta\,D_{\mathrm{KL}}\big(q_\phi(z\mid x)\,\Vert\,p(z)\big)
```

第一项要求 Decoder 保留足够信息重构输入；第二项把每个样本的后验拉向先验 $p(z)$，通常取标准正态分布。训练完成后可以直接从先验采样 $z\sim p(z)$，再经过 Decoder 生成新图像。$\beta$ 控制重构保真度与潜空间规整程度之间的权衡，过大可能导致细节丢失或 posterior collapse，过小则潜空间不够连续。

<h2 id="q-007">面试问题：什么是重参数化技巧，为什么 VAE 必须使用它？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

直接从 $z\sim\mathcal{N}(\mu,\sigma^2)$ 采样会把随机操作放在计算图中，梯度无法稳定地回传到 $\mu$ 和 $\sigma$。重参数化把随机性移到一个与网络参数无关的噪声变量：

```math
\epsilon\sim\mathcal{N}(0,I),\qquad z=\mu+\sigma\odot\epsilon
```

这样 $\mu$、$\sigma$ 仍是可微网络输出，随机性由独立采样的 $\epsilon$ 承担，损失可以通过 $z$ 反向传播到 Encoder。它并没有消除随机性，而是把随机变量和可学习参数解耦。

工程上要注意数值稳定：网络通常预测 $\log\sigma^2$，再通过指数或 softplus 得到正方差；训练时要监控 KL 项，防止 Decoder 过强导致潜变量被忽略。

<h2 id="q-008">面试问题：VAE 生成的图像为什么容易模糊？如何缓解？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

模糊通常来自三个因素：像素级 MSE/L2 重构会对多种可能结果取平均；KL 正则要求潜空间规整，可能牺牲高频细节；Encoder 的下采样和 latent 通道数过小会造成不可逆的信息损失。

缓解方法包括：

- 使用 L1、感知损失或对抗损失，减少像素平均带来的过度平滑。
- 增大 latent 容量或采用分层 latent，让结构和纹理分别编码。
- 使用更强 Decoder、残差块和多尺度重构，提高局部细节恢复能力。
- 让后续扩散模型在 VAE latent 上补足高频内容，而不是要求 VAE 单独生成最终图像。

在 Stable Diffusion 中，VAE 的主要职责是高效压缩和还原 latent；最终视觉质量还由文本条件、去噪网络、采样器和解码阶段共同决定，所以“VAE 重构更清晰”不一定意味着下游生成一定更好。

<h1 id="q-009">3. 为什么 Stable Diffusion 等系统需要 VAE？</h1>

<h2 id="q-010">面试问题：Diffusion Model 和 VAE 中的重参数化分别如何使用？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

两者都利用重参数化让随机过程可训练，但服务的对象不同。VAE 对后验分布采样，把 `z` 写成 `mu+sigma*epsilon`，目的是让重构损失可以更新 Encoder。扩散模型的前向过程则可以直接用闭式公式从干净样本 `x_0` 得到任意时间步的带噪样本：

```math
x_t=\sqrt{\bar{\alpha}_t}\,x_0+
\sqrt{1-\bar{\alpha}_t}\,\epsilon,
\qquad \epsilon\sim\mathcal{N}(0,I)
```

这里的随机噪声用于构造训练输入，模型学习预测噪声、干净样本或速度场；它不是 VAE 意义上的潜变量后验。若在 latent diffusion 中使用 VAE，则先把图像编码为 `z_0`，再在 latent 空间执行扩散，最后由 Decoder 还原像素图。

<h2 id="q-011">面试问题：视觉 tokenizer 的重构质量为什么不等于生成质量？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

重构指标只回答“编码后能否还原输入”，生成模型还要回答“latent 是否容易被学习、采样和控制”。一个 VAE 可能在 PSNR、SSIM 上表现不错，却因为 latent 分布复杂、通道冗余或语义结构不稳定，导致扩散模型训练困难、采样轨迹不平滑或条件控制不一致。

因此 tokenizer 评估至少要分三层：

1. **重构层**：像素误差、感知质量、边缘和文字细节。
2. **表征层**：latent 的规整性、语义可分性、压缩率和跨域稳定性。
3. **生成层**：下游扩散/自回归模型的收敛速度、样本质量、可控性、推理成本和编辑一致性。

这也是视觉基础模型对齐型 VAE、VQ-VAE tokenizer 和高分辨率 latent 研究持续出现的原因：视觉 tokenizer 已经不是简单的图像压缩器，而是生成系统的数据接口和表示瓶颈。

## 高频速记

- AE 负责确定性重构，VAE 通过 KL 正则获得可采样的连续潜空间。
- VQ-VAE 将连续特征量化成离散码本，适合视觉 token 化。
- VAE 的 ELBO 由重构项和 KL 项组成，二者控制保真度与规整程度。
- 重参数化把随机采样写成可微参数加独立噪声。
- VAE 模糊常来自像素平均、KL 正则和 latent 容量不足。
- Stable Diffusion 在 VAE latent 中扩散，用更低计算量完成生成。
- tokenizer 的重构质量、表征质量和下游生成质量必须分开评估。

## 参考资料

- Kingma and Welling, Auto-Encoding Variational Bayes
- van den Oord et al., Neural Discrete Representation Learning
- Rombach et al., High-Resolution Image Synthesis with Latent Diffusion Models
