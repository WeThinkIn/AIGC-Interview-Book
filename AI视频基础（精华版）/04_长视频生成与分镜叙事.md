# 4.4 长视频生成与分镜叙事高频考点

> 本章聚焦长视频生成与分镜叙事中的底层技术问题：长程依赖、漂移、上下文爆炸、分段生成、记忆机制、跨镜头一致性、可控条件传播与评估。相比追踪短期模型名，本章更强调多年后仍然成立的经典问题和工程取舍。

---

# 目录导航

[4.4.1 长视频生成的核心难点：长程依赖、漂移与上下文爆炸](#4.4.1_长视频生成的核心难点：长程依赖、漂移与上下文爆炸)
  - [4.4.1.1 面试问题：为什么长视频生成不能简单理解为“短视频生成跑更久”？](#4.4.1.1_面试问题：为什么长视频生成不能简单理解为“短视频生成跑更久”？)
  - [4.4.1.2 面试问题：长程依赖和短时一致性有什么区别？](#4.4.1.2_面试问题：长程依赖和短时一致性有什么区别？)
  - [4.4.1.3 面试问题：长视频生成中的漂移问题本质是什么？](#4.4.1.3_面试问题：长视频生成中的漂移问题本质是什么？)
  - [4.4.1.4 面试问题：为什么上下文爆炸会成为长视频生成的核心瓶颈？](#4.4.1.4_面试问题：为什么上下文爆炸会成为长视频生成的核心瓶颈？)

[4.4.2 长视频扩展的技术路径：Causal、Chunk、Sliding Window 与 Hierarchical](#4.4.2_长视频扩展的技术路径：Causal、Chunk、Sliding-Window-与-Hierarchical)
  - [4.4.2.1 面试问题：长视频生成常见技术路径有哪些？它们分别解决什么问题？](#4.4.2.1_面试问题：长视频生成常见技术路径有哪些？它们分别解决什么问题？)
  - [4.4.2.2 面试问题：Causal Video Generation 为什么适合流式生成？](#4.4.2.2_面试问题：Causal-Video-Generation-为什么适合流式生成？)
  - [4.4.2.3 面试问题：Chunk 与 Sliding Window 的核心区别是什么？](#4.4.2.3_面试问题：Chunk-与-Sliding-Window-的核心区别是什么？)
  - [4.4.2.4 面试问题：Hierarchical Generation 如何兼顾全局规划和局部质量？](#4.4.2.4_面试问题：Hierarchical-Generation-如何兼顾全局规划和局部质量？)

[4.4.3 跨片段一致性：状态记忆、参考库与边界对齐](#4.4.3_跨片段一致性：状态记忆、参考库与边界对齐)
  - [4.4.3.1 面试问题：长视频生成中到底需要记住什么？](#4.4.3.1_面试问题：长视频生成中到底需要记住什么？)
  - [4.4.3.2 面试问题：Reference Bank、Memory Token 和 State Propagation 分别解决什么问题？](#4.4.3.2_面试问题：Reference-Bank、Memory-Token-和-State-Propagation-分别解决什么问题？)
  - [4.4.3.3 面试问题：Chunk 边界为什么容易出问题？如何缓解边界不连续？](#4.4.3.3_面试问题：Chunk-边界为什么容易出问题？如何缓解边界不连续？)
  - [4.4.3.4 面试问题：为什么长视频不能简单保存所有历史帧？](#4.4.3.4_面试问题：为什么长视频不能简单保存所有历史帧？)

[4.4.4 多镜头与分镜叙事生成：Storyboard、Keyframe 与 Shot-level Planning](#4.4.4_多镜头与分镜叙事生成：Storyboard、Keyframe-与-Shot-level-Planning)
  - [4.4.4.1 面试问题：Multi-shot Video Generation 和单镜头长视频有什么本质区别？](#4.4.4.1_面试问题：Multi-shot-Video-Generation-和单镜头长视频有什么本质区别？)
  - [4.4.4.2 面试问题：Storyboard、Keyframe 和 Shot Video 分别承担什么角色？](#4.4.4.2_面试问题：Storyboard、Keyframe-和-Shot-Video-分别承担什么角色？)
  - [4.4.4.3 面试问题：Story-level Coherence 为什么比单个 shot 质量更难？](#4.4.4.3_面试问题：Story-level-Coherence-为什么比单个-shot-质量更难？)
  - [4.4.4.4 面试问题：多镜头生成中的镜头切换为什么难？](#4.4.4.4_面试问题：多镜头生成中的镜头切换为什么难？)

[4.4.5 长视频可控生成：Camera、Motion、Subject、Structure 的统一条件传播](#4.4.5_长视频可控生成：Camera、Motion、Subject、Structure-的统一条件传播)
  - [4.4.5.1 面试问题：长视频可控生成需要控制哪些核心变量？](#4.4.5.1_面试问题：长视频可控生成需要控制哪些核心变量？)
  - [4.4.5.2 面试问题：Camera Control 和 Motion Control 的区别是什么？](#4.4.5.2_面试问题：Camera-Control-和-Motion-Control-的区别是什么？)
  - [4.4.5.3 面试问题：Subject Control 和 Structure Control 如何跨时间持续生效？](#4.4.5.3_面试问题：Subject-Control-和-Structure-Control-如何跨时间持续生效？)
  - [4.4.5.4 面试问题：多条件控制发生冲突时，模型应如何处理？](#4.4.5.4_面试问题：多条件控制发生冲突时，模型应如何处理？)

[4.4.6 长视频生成评估：长时一致性、叙事一致性与条件遵循](#4.4.6_长视频生成评估：长时一致性、叙事一致性与条件遵循)
  - [4.4.6.1 面试问题：为什么长视频不能只沿用短视频评估指标？](#4.4.6.1_面试问题：为什么长视频不能只沿用短视频评估指标？)
  - [4.4.6.2 面试问题：长时一致性应该如何评估？](#4.4.6.2_面试问题：长时一致性应该如何评估？)
  - [4.4.6.3 面试问题：自动指标和人工评测应该如何结合？](#4.4.6.3_面试问题：自动指标和人工评测应该如何结合？)

---

<h1 id="4.4.1_长视频生成的核心难点：长程依赖、漂移与上下文爆炸">4.4.1 长视频生成的核心难点：长程依赖、漂移与上下文爆炸</h1>

> 本节从两条主线解释长视频生成为什么难：模型能否保存并利用长期状态，以及计算预算能否承载不断增长的时空上下文。漂移是这些问题在生成结果中的典型表现。

<h2 id="4.4.1.1_面试问题：为什么长视频生成不能简单理解为“短视频生成跑更久”？">4.4.1.1 面试问题：为什么长视频生成不能简单理解为“短视频生成跑更久”？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

把短视频模型直接延长推理时间，通常只能增加输出长度，不能自动获得长期状态保持能力。时间范围扩大以后，模型需要持续维护主体、场景、物体状态和事件进度，还要承担更高的计算与显存成本。

**时间拉长后，问题会发生三种变化**

- **依赖范围变长**：短片段主要依赖邻近帧；长视频中的当前内容还可能受更早的角色状态、空间关系和事件结果约束。
- **生成分布发生变化**：采用 causal 或 chunk 递推时，模型会逐步接触自己的历史输出。训练时若主要看到短片段或更干净的上下文，推理越长，训练与推理的差异越容易暴露。
- **时空 token 持续增长**：更长的时间轴会扩大注意力、激活存储和采样开销，完整保留历史通常不可行。

还要先区分任务设定。**单镜头长视频**强调连续时间中的运动和状态延续；**多镜头长视频**允许视角、景别和时间跳跃，但需要额外维护角色、场景与事件逻辑。后者将在 4.4.4 中展开。

上述变化之间的关系如图4-1所示。

> **配图占位（图4-1）**：展示视频时长增加后形成的两条链路：一条是依赖范围扩大，进而要求长期状态记忆和事件规划；另一条是时空 token 增长，进而带来计算、显存与信息筛选压力。图中补充 causal/chunk 递推可能产生的误差反馈环。

工程上，“能连续出帧”只能证明模型具备长度扩展能力。还要检查远距离重现主体时身份是否稳定、事件状态是否被重置、片段边界是否连续，以及后段是否重复或静态化。这些现象共同改善，才支持模型有效利用了长期上下文；要定位具体由哪种记忆机制起作用，还需要相应的条件消融。

> **面试速答**：延长短视频模型的推理时间，只解决“能生成更长”，没有自动解决长期状态保持。长视频还要处理远距离依赖、递推误差、时空 token 成本和训练与推理长度错位；单镜头侧重连续状态，多镜头还要维护镜头间的事件逻辑。

参考资料：[StreamingT2V](https://arxiv.org/abs/2403.14773)、[FreeNoise](https://arxiv.org/abs/2310.15169)

<h2 id="4.4.1.2_面试问题：长程依赖和短时一致性有什么区别？">4.4.1.2 面试问题：长程依赖和短时一致性有什么区别？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

短时一致性约束邻近帧之间的局部演化，长程依赖约束相隔较远的片段是否仍服从同一组状态和事件关系。两者关注的时间尺度不同，也可能独立失败。

两者的差异见表4-1。

**表4-1 短时一致性与长程依赖的比较**

| 比较维度 | 短时一致性 | 长程依赖 |
| --- | --- | --- |
| 观察范围 | 相邻帧或短时间窗口 | 相隔较远的片段乃至完整视频 |
| 主要约束 | 纹理稳定、动作连续、局部几何与光流合理 | 主体身份、场景布局、物体状态、事件顺序 |
| 典型失败 | flicker、局部形变、动作跳变 | 身份漂移、状态重置、场景或叙事前后矛盾 |
| 检查方式 | 相邻帧差异、短窗口运动与感知特征 | 跨时间锚点比较、状态追踪、事件级检查 |

局部平滑不能推出长期正确：角色可以在每个短窗口内变化得很自然，却在几十秒后变成另一个人。反过来，固定背景或几乎静止的主体可能得到较高的相邻帧一致性，却没有完成要求的运动。因此评估时应把**局部连续性、运动幅度和远距离状态保持**分开。

建模上，邻近帧通常保留较高分辨率的时空信息，用于捕捉细粒度运动；远距离历史更适合压缩为关键帧、状态表示或低分辨率记忆。不同时间尺度的信息应采用不同精度，没必要让每个时刻无差别访问全部历史。

> **面试速答**：短时一致性看相邻帧的纹理、几何和动作是否连续，长程依赖看相隔较远的片段能否保持主体、场景、物体状态和事件关系。局部平滑或主体几乎不动都不能证明长程建模有效，评估时要增加跨时间锚点和状态检查。

参考资料：[StreamingT2V](https://arxiv.org/abs/2403.14773)、[Long-Context Autoregressive Video Modeling](https://arxiv.org/abs/2503.19325)

<h2 id="4.4.1.3_面试问题：长视频生成中的漂移问题本质是什么？">4.4.1.3 面试问题：长视频生成中的漂移问题本质是什么？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

漂移是生成结果相对初始条件、历史状态或目标轨迹的偏差随时间系统性增大。它可以表现为身份、场景、运动方向、物体状态或语义目标逐渐改变，与相邻帧间快速波动的 flicker 不是同一种问题。

**漂移从哪里来**

- **历史误差被再次作为条件**：在 causal 或 chunk 递推中，上一段的偏差会进入下一段。模型若对该偏差敏感，误差可能被保留或放大。
- **长期条件逐渐失去作用**：即使一次处理较长上下文，模型也可能没有有效检索早期信息，或者局部运动条件压过了身份、场景和事件约束。
- **训练与推理长度不匹配**：模型主要在短片段上训练，却在推理时外推到更长范围，位置编码、注意力模式和状态分布都可能超出训练覆盖。

对分段递推系统，可以把片段状态写成：

```math
\hat{s}_i = F(\hat{s}_{i-1}, c_i) + \epsilon_i
```

其中， $\hat{s}_i$ 是第 $i$ 个片段结束后的状态， $c_i$ 是当前条件， $\epsilon_i$ 是本段引入的偏差。关键在于状态转移 $F$ 会抑制、保留还是放大已有偏差，因此总漂移不能简单写成各段误差的线性求和。

**定位漂移要看偏差如何随时间增长**

- 按时间距离绘制身份相似度、状态正确率或轨迹误差，确认偏差是持续增长还是在某个边界突变。
- 对比自由递推与定期注入干净锚点的结果。重置后明显恢复，通常说明历史反馈链是主要问题。
- 分别统计 chunk 内部和边界附近的错误。错误集中在边界，更可能是状态传递或重叠融合不足；全程缓慢恶化，则更像长期条件利用不足。

> **面试速答**：漂移是身份、场景、运动或事件状态相对目标的偏差随时间持续增大。递推生成常见历史误差反馈，一次处理长上下文也可能因长期条件利用不足或长度外推而漂移；定位时应看误差增长曲线，并比较锚点重置、chunk 内部和片段边界的结果。

参考资料：[StreamingT2V](https://arxiv.org/abs/2403.14773)、[Long-Context Autoregressive Video Modeling](https://arxiv.org/abs/2503.19325)

<h2 id="4.4.1.4_面试问题：为什么上下文爆炸会成为长视频生成的核心瓶颈？">4.4.1.4 面试问题：为什么上下文爆炸会成为长视频生成的核心瓶颈？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

上下文瓶颈首先来自时空 token 的乘法增长。设视频经过时空压缩和 patchify 后，时间、空间尺寸分别为 $T'$、$H'$ 和 $W'$，则 token 数近似为：

```math
N = T'H'W'
```

如果主干在全部视频 token 上执行稠密 self-attention，单层注意力计算量近似为：

```math
\mathcal{O}(N^2 d)
```

其中 $d$ 是注意力特征维度。未采用显存优化实现时，注意力矩阵的存储也随 $N^2$ 增长。空间分辨率与视频时长相乘，使长视频训练很快受到计算量和激活显存限制；扩散或流模型还要在多个采样步中重复执行主干网络。

**成本需要按架构分别判断**

- 稠密全时空注意力主要受二次复杂度约束；分解注意力、局部窗口或稀疏注意力会改变实际增长曲线。
- KV cache 是 causal 或自回归实现中的典型成本，不应泛化到所有视频扩散模型。它能减少历史 token 的重复计算，但缓存本身仍会随有效历史增长。
- chunk 方法把单次计算限制在固定窗口内，却把压力转移到跨片段状态传递和边界一致性上。

计算预算解决后，仍有信息利用问题。长视频包含大量相邻帧冗余，远距离历史中真正相关的信息只占一部分；上下文过长时，模型还可能检索不到关键状态，或者反复利用已经出错的历史。实践中通常为近邻上下文保留细粒度信息，将远距离历史压缩为记忆或按需检索，再用高层规划维护事件结构。4.4.2 和 4.4.3 将继续展开这些方案。

> **面试速答**：视频 token 数近似为 $N = T'H'W'$，稠密全时空注意力的主要计算量随 $N^2$ 增长，因此时长和分辨率会共同推高训练与采样成本。KV cache 主要属于 causal 或自回归实现；更常见的取舍是用局部窗口保留短时细节，再用压缩记忆或检索传递远距离状态。

参考资料：[Attention Is All You Need](https://arxiv.org/abs/1706.03762)、[Long-Context Autoregressive Video Modeling](https://arxiv.org/abs/2503.19325)

---

<h1 id="4.4.2_长视频扩展的技术路径：Causal、Chunk、Sliding-Window-与-Hierarchical">4.4.2 长视频扩展的技术路径：Causal、Chunk、Sliding Window 与 Hierarchical</h1>

> 本节讨论长视频系统的四个设计维度：生成依赖、序列切分、上下文范围和规划层级。Causal、Chunk、Sliding Window 与 Hierarchical 可以同时出现在一个系统中，不能当成四种互斥架构。

<h2 id="4.4.2.1_面试问题：长视频生成常见技术路径有哪些？它们分别解决什么问题？">4.4.2.1 面试问题：长视频生成常见技术路径有哪些？它们分别解决什么问题？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

这四个概念分别回答不同问题：后续内容能否读取未来、一次生成多长、每次能看到多少历史，以及是否先做全局规划。表4-2给出了它们的职责边界。

**表4-2 长视频扩展中的四个设计维度**

| 设计维度 | 典型做法 | 主要解决的问题 | 主要代价 |
| --- | --- | --- | --- |
| 生成依赖 | Causal | 允许按时间递推和增量输出 | 缺少未来视觉上下文，递推误差可能累积 |
| 序列切分 | Chunk | 将长序列拆成可承载的生成或处理单元 | 需要处理片段边界和状态传递 |
| 上下文范围 | Sliding Window | 用有限窗口控制单次可见历史和计算量 | 窗口外状态容易丢失 |
| 规划层级 | Hierarchical | 用脚本、关键帧或粗粒度 latent 约束局部生成 | 上层计划与下层结果可能不一致 |

实际系统可以先生成事件或关键帧计划，再由 causal 模型逐段输出；近期细节通过 sliding window 传递，远距离状态则交给压缩记忆或参考检索。组合关系如图4-2所示。

> **配图占位（图4-2）**：展示一条组合式长视频链路：Hierarchical 模块先产生全局事件与关键帧计划，Causal 生成器按 Chunk 递推，Sliding Window 保留近期高精度上下文，长期记忆向后续片段提供主体、场景和事件状态。标出 chunk 边界以及短期窗口和长期记忆的不同作用范围。

选型时先看约束。需要边生成边输出，优先确定 causal 依赖；单次显存装不下完整序列，再决定 chunk 长度和窗口大小；任务涉及多镜头或长事件，还要重点考虑高层计划。顺序反过来，很容易把所有问题都归到“上下文不够长”。

> **面试速答**：Causal、Chunk、Sliding Window 和 Hierarchical 不在同一条分类轴上。它们分别控制时间依赖、生成单元、可见上下文和规划粒度；实际系统可以按需求组合其中多项，再用长期记忆补充窗口之外的状态。

参考资料：[StreamingT2V](https://arxiv.org/abs/2403.14773)、[FreeNoise](https://arxiv.org/abs/2310.15169)、[NUWA-XL](https://arxiv.org/abs/2303.12346)

<h2 id="4.4.2.2_面试问题：Causal-Video-Generation-为什么适合流式生成？">4.4.2.2 面试问题：Causal Video Generation 为什么适合流式生成？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

Causal Video Generation 要求第 $i$ 个生成单元只依赖已生成历史和当前可用条件，不读取未来视频内容。生成单元可以是一帧、一组 latent token，也可以是一个 chunk：

```math
p(X_{1:K} \mid c) = \prod_{i=1}^{K} p(X_i \mid X_{<i}, c)
```

其中， $X_i$ 表示第 $i$ 个生成单元， $c$ 可以包含文本或预先给定的全局计划。这个分解允许系统完成一段就向外输出一段，也能把历史特征放入 cache 或压缩状态中复用，无须等待完整视频生成完毕。

**流式能力需要整条链路成立**

- **生成主干**：注意力掩码或递推结构不能访问未来视频 token。
- **视频编解码器**：若编码或解码当前片段时仍需要未来帧，就无法低延迟地输出。Causal VAE 可以消除这一层对未来帧的依赖。
- **推理系统**：采样步数、缓存更新和后处理延迟必须满足在线输出要求。模型具备 causal 结构，不代表端到端延迟一定足够低。

这里尤其要分清 **causal VAE** 和 **causal generation**。前者只约束编解码的时间感受野；生成主干若仍对整段 latent 做双向去噪，系统就不具备严格的增量生成能力。反过来，主干可以按 chunk 递推，但非 causal 解码器仍会增加输出等待时间。

代价也很明确。causal 依赖缺少未来视觉上下文，长程规划更依赖预先给定的脚本、关键帧或状态计划；递推时还可能出现误差反馈和后段重复。验证时可分别检查主干 attention mask、缓存前后一致性，以及只扰动未来输入时 causal VAE 的早期 latent 是否保持不变。

> **面试速答**：Causal Video Generation 让当前帧或 chunk 只依赖历史，因此可以增量生成并复用缓存。端到端流式输出还要求生成主干、视频编解码器和推理链路都不等待未来内容；只有 causal VAE，不能证明整个生成模型是 causal 的。

参考资料：[Phenaki](https://arxiv.org/abs/2210.02399)、[StreamingT2V](https://arxiv.org/abs/2403.14773)、[CogVideoX](https://arxiv.org/abs/2408.06072)

<h2 id="4.4.2.3_面试问题：Chunk-与-Sliding-Window-的核心区别是什么？">4.4.2.3 面试问题：Chunk 与 Sliding Window 的核心区别是什么？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

Chunk 和 Sliding Window 经常一起出现，但控制的对象不同。

- **Chunk 决定处理或输出单元**：把完整时间轴拆成若干片段，每次生成一个或一组片段。相邻 chunk 可以完全分离，也可以保留重叠帧。
- **Sliding Window 决定可见上下文**：窗口随时间移动，限定当前计算能访问哪些近期帧或 token。它可以服务于 chunk 递推，也可以直接用于长序列的局部注意力或去噪。

因此，chunk 长度、窗口长度和滑动步长是三个不同参数。例如一次生成 24 帧，不代表只能看 24 帧历史；也可以生成 24 帧，同时读取上一段的重叠帧、压缩记忆和外部参考。反过来，模型即使采用滑动窗口，也未必把窗口内所有帧作为一个 chunk 同时输出。

**两类失败也不同**

- Chunk 的典型问题发生在片段交界处，例如动作跳变、身份突变和光照变化。
- Sliding Window 的典型问题来自感受野截断。窗口内部可以很平滑，但早期主体状态或事件结果离开窗口后仍可能被遗忘。

工程上，复用固定长度短视频模型时通常先采用 chunk；需要控制计算量并维护近期连续性时，再增加重叠或 sliding window。若问题是几十秒前的身份和事件状态丢失，继续扩大重叠区的收益通常有限，需要引入长期记忆或参考检索。

> **面试速答**：Chunk 决定一次处理或输出哪一段视频，Sliding Window 决定当前计算能看到哪一段上下文。二者可以组合，重叠也不是 chunk 的必要条件；前者主要处理单次计算规模，后者主要在上下文成本与可见范围之间取舍。

参考资料：[StreamingT2V](https://arxiv.org/abs/2403.14773)、[FreeNoise](https://arxiv.org/abs/2310.15169)

<h2 id="4.4.2.4_面试问题：Hierarchical-Generation-如何兼顾全局规划和局部质量？">4.4.2.4 面试问题：Hierarchical Generation 如何兼顾全局规划和局部质量？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

Hierarchical Generation 先在低频或高抽象层级确定长程结构，再让局部生成器完成运动、纹理和过渡。这样可以把一个高维长序列问题拆成“低分辨率或高抽象的全局计划”和“高质量局部实现”：

```math
p(x_{1:T}, z \mid c) = p(z \mid c) p(x_{1:T} \mid z, c)
```

其中， $z$ 是高层计划，可以是事件序列、镜头描述、稀疏关键帧、低帧率视频或粗粒度 latent。公式描述的是先生成计划、再生成视频的联合过程；具体系统不一定显式建模这个概率分解。

**全局与局部分别承担什么**

- **全局层**关注覆盖范围：先确定事件顺序、镜头结构、关键状态或稀疏视觉锚点，不承担全部像素细节。
- **局部层**关注生成质量：在较短时间范围内补出运动、纹理和转场，并遵守对应的高层状态。

这种分工可以降低单次上下文成本；在区间条件足够时，稀疏关键帧还允许多个区间并行补帧。它并不自动带来全局一致性：高层计划本身可能前后矛盾，局部生成也可能忽略计划，错误还会从上层传到所有下游片段。

排查时要把两层拆开评估。先检查脚本、关键帧或粗视频是否已经覆盖正确的主体、状态和事件顺序；再用人工或真实计划替换模型计划，观察局部质量是否恢复。替换后恢复，瓶颈主要在规划层；计划正确但视频不遵守，则应检查条件表示、注入位置和局部生成器的遵循能力。Storyboard、Keyframe 和 Shot Video 的具体职责留到 4.4.4 展开。

> **面试速答**：Hierarchical Generation 用高层计划覆盖长时间范围，再由局部模型生成运动和细节。它能降低长序列建模压力，也便于并行补全；主要风险是上层计划错误和下层不遵循，因此要分别评估计划质量与条件遵循。

参考资料：[NUWA-XL](https://arxiv.org/abs/2303.12346)、[Hierarchical Video Generation for Complex Data](https://arxiv.org/abs/2106.02719)

---

<h1 id="4.4.3_跨片段一致性：状态记忆、参考库与边界对齐">4.4.3 跨片段一致性：状态记忆、参考库与边界对齐</h1>

> 本节是长视频生成的核心工程问题。生成更长的视频并不难，难的是让不同片段之间保持同一主体、同一场景、同一状态和同一运动过程。

<h2 id="4.4.3.1_面试问题：长视频生成中到底需要记住什么？">4.4.3.1 面试问题：长视频生成中到底需要记住什么？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

长视频生成不需要记住所有历史帧，而需要记住对后续生成有约束作用的状态。主要包括四类：

1. **主体状态**：人物身份、服饰、发型、配饰、物体外观。
2. **场景状态**：空间布局、背景物体、光照、镜头方向。
3. **动作状态**：动作阶段、速度、方向、姿态延续。
4. **事件状态**：哪些事情已经发生，哪些物体已经被移动或改变。

可以把历史信息压缩为状态记忆：

```math
\mathcal{M}_t=g(x_{1:t}, c)
```

生成后续片段时，不一定访问完整历史，而是访问记忆：

```math
x_{t+1:t+L}\sim p(x_{t+1:t+L}\mid \mathcal{M}_t,c)
```

面试回答应强调：**长视频一致性依赖状态记忆，而不是无限上下文。真正关键的是记住稳定身份、场景结构和事件状态。**

<h2 id="4.4.3.2_面试问题：Reference-Bank、Memory-Token-和-State-Propagation-分别解决什么问题？">4.4.3.2 面试问题：Reference Bank、Memory Token 和 State Propagation 分别解决什么问题？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

这三类机制都用于跨片段一致性，但关注点不同。

| 机制 | 核心作用 | 典型信息 |
|---|---|---|
| Reference Bank | 保存可检索视觉参考 | 人脸、服饰、场景、关键帧 |
| Memory Token | 用紧凑 token 表示历史状态 | 主体状态、场景摘要、动作阶段 |
| State Propagation | 将上一片段状态传给下一片段 | 尾帧、latent、pose、trajectory |

Reference Bank 更像“外部参考库”，适合跨镜头主体保持和场景复用。Memory Token 更像“压缩历史”，用于减少完整历史上下文成本。State Propagation 更像“递推状态”，常用于 chunk 或 causal 生成中衔接前后片段。

抽象地说，生成第 $i$ 个片段时可以写成：

```math
x^{(i)}\sim p(x^{(i)}\mid c_i,\mathrm{Retrieve}(\mathcal{R},c_i),m_{i-1},s_{i-1})
```

其中 $\mathcal{R}$ 是参考库，$m_{i-1}$ 是历史 memory，$s_{i-1}$ 是上一片段状态。面试中可以概括为：**Reference Bank 负责查，Memory Token 负责压缩，State Propagation 负责传递。**

<h2 id="4.4.3.3_面试问题：Chunk-边界为什么容易出问题？如何缓解边界不连续？">4.4.3.3 面试问题：Chunk 边界为什么容易出问题？如何缓解边界不连续？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

Chunk 边界容易出问题，是因为相邻片段往往在不同去噪过程、不同随机噪声或不同局部上下文下生成。即使每个片段单独质量较好，拼接处也可能出现动作跳变、身份变化、光照变化或语义断裂。

常见缓解方法包括：

1. **Overlap generation**：相邻 chunk 保留重叠帧，让模型在交界处共享上下文。
2. **Boundary conditioning**：用上一段尾帧、尾部 latent 或 motion state 作为下一段条件。
3. **Keyframe anchoring**：为每个片段提供首尾关键帧，约束片段起止状态。
4. **Feature / latent consistency loss**：训练阶段约束边界附近的特征连续。
5. **Post blending**：推理后对重叠区域做融合，但它只能缓解视觉跳变，不能解决语义断裂。

可以把边界一致性理解为约束：

```math
d(\phi(x^{(i)}_{\text{tail}}),\phi(x^{(i+1)}_{\text{head}})) < \tau
```

其中 $\phi$ 可以是视觉特征、身份特征、pose、motion 或语义特征。面试中可以总结为：**chunk 边界不是剪辑问题，而是状态连续性问题。**

<h2 id="4.4.3.4_面试问题：为什么长视频不能简单保存所有历史帧？">4.4.3.4 面试问题：为什么长视频不能简单保存所有历史帧？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

保存所有历史帧看似可以解决遗忘问题，但在长视频中通常不可行，原因有三点。

第一，计算和显存成本过高。历史帧越多，token 数和 attention 成本越大。第二，历史信息存在冗余，大量相邻帧并不会提供新的长期约束。第三，历史中可能包含错误帧，若全部保留，错误会被后续生成反复引用并放大。

更合理的方式是做历史压缩和检索：

```math
\mathcal{M}_t=\mathrm{Compress}(x_{1:t})
```

再根据当前生成目标检索相关记忆：

```math
r_t=\mathrm{Retrieve}(\mathcal{M}_t,c_t)
```

面试中可以概括为：**长视频需要的是选择性记忆，而不是完整录像回放。关键是保留身份、场景、状态和事件这些长期约束信息。**

---

<h1 id="4.4.4_多镜头与分镜叙事生成：Storyboard、Keyframe-与-Shot-level-Planning">4.4.4 多镜头与分镜叙事生成：Storyboard、Keyframe 与 Shot-level Planning</h1>

> 本节关注多镜头视频。它比单镜头长视频更难，因为镜头之间允许视角、景别和时间跳跃，但不允许角色、场景和事件逻辑无故断裂。

<h2 id="4.4.4.1_面试问题：Multi-shot-Video-Generation-和单镜头长视频有什么本质区别？">4.4.4.1 面试问题：Multi-shot Video Generation 和单镜头长视频有什么本质区别？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

单镜头长视频强调一个连续时间轴上的持续生成：

```math
x_{1:T}
```

Multi-shot Video Generation 则生成多个镜头片段：

```math
X=\{S_1,S_2,\ldots,S_K\}
```

每个 $S_i$ 可以有不同视角、景别、构图和动作阶段，但它们共同服务于同一个故事或表达目标。

二者的本质区别在于：单镜头长视频主要解决连续时间演化，多镜头生成还要解决镜头组织、跨镜头主体一致性、场景连续性和事件因果。单个 shot 质量高，并不意味着整个多镜头视频成立。

面试中可以概括为：**单镜头长视频考察时间连续性，多镜头生成考察镜头级组织和故事级一致性。**

<h2 id="4.4.4.2_面试问题：Storyboard、Keyframe-和-Shot-Video-分别承担什么角色？">4.4.4.2 面试问题：Storyboard、Keyframe 和 Shot Video 分别承担什么角色？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

分镜叙事生成通常采用分层流程：

```text
Script -> Storyboard -> Keyframe -> Shot Video -> Final Video
```

各层职责不同：

| 层级 | 作用 | 解决问题 |
|---|---|---|
| Script | 描述故事、角色、事件顺序 | 语义规划 |
| Storyboard | 拆分镜头、确定景别和镜头顺序 | 镜头规划 |
| Keyframe | 提供首帧、尾帧或关键视觉状态 | 视觉锚定 |
| Shot Video | 生成每个镜头的动态片段 | 局部视频生成 |

这种分层的核心价值是降低问题难度。模型不需要一次性从长文本生成完整视频，而是先得到结构化计划，再逐镜头生成。

需要注意的是，分层并不自动保证正确。Script 到 Storyboard 可能丢失语义，Storyboard 到 Keyframe 可能构图错误，Keyframe 到 Shot Video 可能动作不连续。因此分镜生成的关键是上下层对齐。

<h2 id="4.4.4.3_面试问题：Story-level-Coherence-为什么比单个-shot-质量更难？">4.4.4.3 面试问题：Story-level Coherence 为什么比单个 shot 质量更难？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

Story-level Coherence 指多个镜头组合后，在角色、场景、事件、镜头语言和视觉风格上形成一致整体。它比单个 shot 质量更难，因为它评价的是镜头之间的关系。

如果独立生成每个镜头，可以写成：

```math
\prod_{i=1}^{K}p(S_i\mid c_i)
```

这种方式容易忽略跨镜头依赖。更合理的形式应考虑故事上下文和历史镜头：

```math
p(S_i\mid S_{<i},c_i,c_{\text{story}})
```

Story-level Coherence 至少包括四类一致性：角色一致、场景一致、事件因果一致、视觉风格一致。面试中可以总结为：**shot-level quality 关注单个镜头是否好看，story-level coherence 关注所有镜头合起来是否像同一个故事。**

<h2 id="4.4.4.4_面试问题：多镜头生成中的镜头切换为什么难？">4.4.4.4 面试问题：多镜头生成中的镜头切换为什么难？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

镜头切换难，是因为它既要允许视角、景别或时间发生变化，又不能破坏主体、空间和事件连续性。真实影视中镜头切换遵循空间轴线、动作连续、视线匹配和叙事节奏；生成模型如果缺乏这些约束，就容易像随机片段拼接。

常见问题包括：同一人物跨镜头换脸、同一场景空间方向反转、动作阶段倒退、物体状态被重置、事件缺少承接。

可以把镜头生成写成：

```math
S_i \sim p(S_i \mid S_{i-1}, c_i, c_{\text{global}})
```

其中 $c_{\text{global}}$ 包含角色、场景、事件和风格约束。面试中可以概括为：**镜头切换不是简单拼接片段，而是在允许变化的同时维持可解释的连续性。**

---

<h1 id="4.4.5_长视频可控生成：Camera、Motion、Subject、Structure-的统一条件传播">4.4.5 长视频可控生成：Camera、Motion、Subject、Structure 的统一条件传播</h1>

> 本节关注可控生成。长视频可控性的核心不是“加更多条件”，而是让条件在时间维度上持续生效，并在多条件之间保持一致。

<h2 id="4.4.5.1_面试问题：长视频可控生成需要控制哪些核心变量？">4.4.5.1 面试问题：长视频可控生成需要控制哪些核心变量？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

长视频可控生成通常需要控制四类变量：

| 控制类型 | 控制对象 | 典型条件 |
|---|---|---|
| Camera | 观察方式 | pan、tilt、zoom、dolly、camera pose |
| Motion | 主体或区域如何运动 | trajectory、pose、flow、motion strength |
| Subject | 生成谁 | reference image、identity embedding、subject token |
| Structure | 画面结构 | layout、depth、mask、bbox、segmentation |

可以写成统一条件生成形式：

```math
p(x_{1:T}\mid c_{\text{text}},c_{\text{camera}},c_{\text{motion}},c_{\text{subject}},c_{\text{structure}})
```

长视频中的难点不是条件种类多，而是条件必须持续约束整段视频。例如主体身份不能只在首帧正确，相机轨迹不能只在开头生效，结构条件不能中途漂移。

面试中可以总结为：**长视频可控生成的关键是条件的时间传播和跨条件协调。**

<h2 id="4.4.5.2_面试问题：Camera-Control-和-Motion-Control-的区别是什么？">4.4.5.2 面试问题：Camera Control 和 Motion Control 的区别是什么？</h2>

**难度评分：⭐⭐⭐ (3/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

Camera Control 控制“怎么看”，Motion Control 控制“怎么动”。二者都会造成画面运动，但物理含义不同。

Camera Control 约束相机视角和镜头运动，例如推近、拉远、平移、旋转、环绕。主体不动但相机移动，画面仍会产生整体运动。

Motion Control 约束主体、局部区域或场景元素的运动，例如人体姿态序列、主体轨迹、光流方向或动作强度。相机不动但主体移动，也会产生运动。

区别可以概括为：

| 类型 | 关注对象 | 常见条件 |
|---|---|---|
| Camera Control | 观察坐标系变化 | camera pose、trajectory、zoom |
| Motion Control | 被观察对象变化 | pose、flow、trajectory、action reference |

长视频中二者必须协调。若相机向左移动、主体向右移动，最终光流可能相互抵消或叠加。模型需要区分 camera-induced motion 和 object-induced motion，否则容易出现背景扭曲、主体漂移或空间不稳定。

<h2 id="4.4.5.3_面试问题：Subject-Control-和-Structure-Control-如何跨时间持续生效？">4.4.5.3 面试问题：Subject Control 和 Structure Control 如何跨时间持续生效？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

Subject Control 约束“是谁”，Structure Control 约束“画面结构是什么”。二者都需要跨时间持续生效。

Subject Control 常依赖参考图、身份 embedding、reference attention 或 subject token。其目标是维持人物、商品、动物或角色的身份和关键外观。Structure Control 常依赖 layout、depth、pose、mask、trajectory 等条件，用于约束空间布局、几何关系和姿态结构。

二者可以抽象为：

```math
p(x_{1:T}\mid c_{\text{text}},c_{\text{subject}},c_{\text{structure}})
```

跨时间生效的关键包括：参考特征持续注入、关键帧锚定、历史状态检索、结构条件序列化，以及对生成结果进行一致性约束。

面试中应避免只说“加参考图”或“加 ControlNet”。更准确的回答是：**Subject Control 需要保持身份记忆，Structure Control 需要保持几何和布局约束；长视频场景下，二者都必须在时间维度上传播。**

<h2 id="4.4.5.4_面试问题：多条件控制发生冲突时，模型应如何处理？">4.4.5.4 面试问题：多条件控制发生冲突时，模型应如何处理？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

长视频生成中，多条件冲突很常见。例如文本要求角色转身，参考图只提供正脸；相机轨迹要求左移，主体轨迹要求右移；pose 条件和身份参考在局部区域不一致。

处理思路通常包括：

1. **条件分层**：高层语义、主体身份、几何结构、局部运动分别进入不同条件通道。
2. **条件加权**：根据任务目标设置 guidance scale 或动态权重。
3. **冲突检测**：识别明显不一致的参考或控制信号并降权。
4. **时间调度**：不同阶段使用不同条件强度，例如首帧更强身份约束，后续更强运动约束。
5. **局部化控制**：通过 mask 或区域条件限制控制影响范围。

可抽象为多条件融合：

```math
h=\sum_i \alpha_i h_i,\quad \sum_i \alpha_i=1
```

其中 $h_i$ 是不同条件特征，$\alpha_i$ 是条件权重。面试中可以概括为：**多条件控制的关键不是把条件全部塞进模型，而是解决条件优先级、作用范围和时间调度。**

---

<h1 id="4.4.6_长视频生成评估：长时一致性、叙事一致性与条件遵循">4.4.6 长视频生成评估：长时一致性、叙事一致性与条件遵循</h1>

> 本节关注评估。长视频质量是多维问题，不能被单帧画质或短时运动指标概括。

<h2 id="4.4.6.1_面试问题：为什么长视频不能只沿用短视频评估指标？">4.4.6.1 面试问题：为什么长视频不能只沿用短视频评估指标？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

短视频指标主要关注单帧质量、短时运动自然度、文本对齐和局部 flicker。长视频还需要评估长期主体一致性、场景稳定性、事件因果、镜头组织和条件持续遵循。

可以把长视频质量写成多维函数：

```math
Q=f(Q_{\text{visual}},Q_{\text{motion}},Q_{\text{consistency}},Q_{\text{story}},Q_{\text{control}})
```

其中视觉质量只是其中一部分。一个视频可以单帧很好，但角色中途变脸；也可以 prompt 对齐很好，但镜头之间缺少叙事关系。因此长视频评估必须从局部质量扩展到全局一致性。

面试中可以概括为：**短视频评估看一段是否像视频，长视频评估看整段是否像同一个作品。**

<h2 id="4.4.6.2_面试问题：长时一致性应该如何评估？">4.4.6.2 面试问题：长时一致性应该如何评估？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

长时一致性评估关注远距离时间点之间是否仍保持同一主体、同一场景、同一状态和同一语义目标。它不是只看相邻帧平滑。

可以用特征相似度描述：

```math
\mathrm{Consistency}(t_i,t_j)=\mathrm{sim}(\phi(x_{t_i}),\phi(x_{t_j}))
```

其中 $\phi$ 可以是身份特征、场景特征、CLIP / VLM 特征、pose 特征或 motion 特征。

具体评估维度包括：

1. **身份一致性**：人物、动物或商品是否漂移。
2. **场景一致性**：布局、背景物体和光照是否稳定。
3. **运动一致性**：动作方向、速度和阶段是否连续。
4. **语义一致性**：是否长期符合 prompt、script 或 storyboard。
5. **状态一致性**：物体状态和事件结果是否被保留。

面试中可以总结为：**长时一致性评估的核心是跨时间比较，而不是只检查相邻帧。**

<h2 id="4.4.6.3_面试问题：自动指标和人工评测应该如何结合？">4.4.6.3 面试问题：自动指标和人工评测应该如何结合？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

长视频生成需要自动指标和人工评测结合。自动指标可规模化、可复现，适合模型迭代；人工评测更擅长判断叙事连贯、可观看性和高层语义错误。

自动指标可覆盖：视觉质量、运动平滑、文本对齐、身份相似度、轨迹误差、pose error、storyboard 对齐、音画同步等。人工评测应覆盖：主体是否稳定、故事是否连贯、镜头是否自然、控制是否持续生效、整体是否可用。

更合理的评估形式不是单一总分，而是多维 profile：

```math
Q=\{Q_{\text{visual}},Q_{\text{motion}},Q_{\text{text}},Q_{\text{identity}},Q_{\text{story}},Q_{\text{control}}\}
```

单一分数不可靠，因为不同维度可能互相冲突。例如画质高但主体漂移，或文本对齐好但动作不自然。面试中可以概括为：**自动指标负责规模化筛选，人工评测负责高层一致性和可用性判断。**
