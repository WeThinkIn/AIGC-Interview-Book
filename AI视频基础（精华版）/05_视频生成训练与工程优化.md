# 4.5 AI 视频生成的训练与工程优化核心高频考点

> 本章聚焦视频生成模型训练和部署中的真实工程痛点：数据管线、Video VAE、token budget、混合训练、训练稳定性、分布式效率、推理加速、质量评估与线上服务。这里不再重复视频基础概念，而是从工程师实战视角分析：模型为什么训不稳、为什么吞吐上不去、为什么显存爆、为什么生成慢、怎样判断模型是否真的变好，以及如何定位和优化。

---

# 目录导航

[4.5.1 视频数据管线：数据质量、解码成本与 caption 对齐](#4.5.1_视频数据管线：数据质量、解码成本与-caption-对齐)
  - [4.5.1.1 面试问题：训练视频生成模型时，数据管线最容易拖垮哪些环节？](#4.5.1.1_面试问题：训练视频生成模型时，数据管线最容易拖垮哪些环节？)
  - [4.5.1.2 面试问题：视频切片、去重和镜头切分为什么会直接影响训练效果？](#4.5.1.2_面试问题：视频切片、去重和镜头切分为什么会直接影响训练效果？)
  - [4.5.1.3 面试问题：高质量 video caption 应该如何服务于 T2V 训练？](#4.5.1.3_面试问题：高质量-video-caption-应该如何服务于-T2V-训练？)

[4.5.2 Token Budget 与训练规格：Video VAE、帧数、分辨率和 bucketing](#4.5.2_Token-Budget-与训练规格：Video-VAE、帧数、分辨率和-bucketing)
  - [4.5.2.1 面试问题：为什么 token budget 是视频生成训练的核心约束？](#4.5.2.1_面试问题：为什么-token-budget-是视频生成训练的核心约束？)
  - [4.5.2.2 面试问题：Video VAE 的压缩率会如何影响训练和生成质量？](#4.5.2.2_面试问题：Video-VAE-的压缩率会如何影响训练和生成质量？)
  - [4.5.2.3 面试问题：训练中如何设计帧数、分辨率、帧率和长宽比的 bucketing？](#4.5.2.3_面试问题：训练中如何设计帧数、分辨率、帧率和长宽比的-bucketing？)

[4.5.3 训练 Recipe：图像-视频混合、课程学习与阶段化训练](#4.5.3_训练-Recipe：图像-视频混合、课程学习与阶段化训练)
  - [4.5.3.1 面试问题：图像-视频联合训练的混合比例应该如何理解？](#4.5.3.1_面试问题：图像-视频联合训练的混合比例应该如何理解？)
  - [4.5.3.2 面试问题：为什么视频生成训练通常需要 progressive training？](#4.5.3.2_面试问题：为什么视频生成训练通常需要-progressive-training？)
  - [4.5.3.3 面试问题：高质量微调和偏好后训练主要解决什么问题？](#4.5.3.3_面试问题：高质量微调和偏好后训练主要解决什么问题？)

[4.5.4 训练稳定性：噪声调度、条件 dropout、loss weighting 与异常排查](#4.5.4_训练稳定性：噪声调度、条件-dropout、loss-weighting-与异常排查)
  - [4.5.4.1 面试问题：视频扩散或 Flow Matching 训练中常见不稳定现象有哪些？](#4.5.4.1_面试问题：视频扩散或-Flow-Matching-训练中常见不稳定现象有哪些？)
  - [4.5.4.2 面试问题：Diffusion 与 Flow Matching 在工程训练上各有什么关注点？](#4.5.4.2_面试问题：Diffusion-与-Flow-Matching-在工程训练上各有什么关注点？)
  - [4.5.4.3 面试问题：CFG dropout、EMA、timestep sampling 和 loss weighting 分别解决什么问题？](#4.5.4.3_面试问题：CFG-dropout、EMA、timestep-sampling-和-loss-weighting-分别解决什么问题？)

[4.5.5 分布式训练效率：数据吞吐、显存优化与并行策略](#4.5.5_分布式训练效率：数据吞吐、显存优化与并行策略)
  - [4.5.5.1 面试问题：视频训练吞吐低，应该从哪些环节排查？](#4.5.5.1_面试问题：视频训练吞吐低，应该从哪些环节排查？)
  - [4.5.5.2 面试问题：视频模型训练中如何做显存优化，而不是简单减 batch？](#4.5.5.2_面试问题：视频模型训练中如何做显存优化，而不是简单减-batch？)
  - [4.5.5.3 面试问题：数据并行、张量并行、序列并行和 FSDP 如何组合？](#4.5.5.3_面试问题：数据并行、张量并行、序列并行和-FSDP-如何组合？)

[4.5.6 推理部署优化：采样加速、VAE 解码、服务调度与成本控制](#4.5.6_推理部署优化：采样加速、VAE-解码、服务调度与成本控制)
  - [4.5.6.1 面试问题：视频生成线上推理为什么常常慢在不止一个地方？](#4.5.6.1_面试问题：视频生成线上推理为什么常常慢在不止一个地方？)
  - [4.5.6.2 面试问题：视频生成推理加速有哪些工程路线？各自代价是什么？](#4.5.6.2_面试问题：视频生成推理加速有哪些工程路线？各自代价是什么？)
  - [4.5.6.3 面试问题：工业部署中如何设计质量、延迟、吞吐和成本的分层策略？](#4.5.6.3_面试问题：工业部署中如何设计质量、延迟、吞吐和成本的分层策略？)

[4.5.7 视频生成评估体系：多维质量、人类偏好与工程闭环](#4.5.7_视频生成评估体系：多维质量、人类偏好与工程闭环)
  - [4.5.7.1 面试问题：为什么视频生成不能依赖单一指标判断模型好坏？](#4.5.7.1_面试问题：为什么视频生成不能依赖单一指标判断模型好坏？)
  - [4.5.7.2 面试问题：一个视频生成评估体系应包含哪些核心维度？](#4.5.7.2_面试问题：一个视频生成评估体系应包含哪些核心维度？)
  - [4.5.7.3 面试问题：自动指标、人类评测和多模态大模型评审应如何分工？](#4.5.7.3_面试问题：自动指标、人类评测和多模态大模型评审应如何分工？)
  - [4.5.7.4 面试问题：模型离线分数提升但用户体验没有变好，应如何排查？](#4.5.7.4_面试问题：模型离线分数提升但用户体验没有变好，应如何排查？)
  - [4.5.7.5 面试问题：如何把视频评估接入训练、发布和线上质量优化闭环？](#4.5.7.5_面试问题：如何把视频评估接入训练、发布和线上质量优化闭环？)

---

<h1 id="4.5.1_视频数据管线：数据质量、解码成本与-caption-对齐">4.5.1 视频数据管线：数据质量、解码成本与 caption 对齐</h1>

> 视频数据管线要同时交付两样东西：语义和时序关系可靠的训练样本，以及持续、稳定的数据吞吐。模型不收敛、运动偏弱或文本不跟随，有时并非结构本身的问题，而是样本构造、caption 对齐或数据供给出了偏差。

<h2 id="4.5.1.1_面试问题：训练视频生成模型时，数据管线最容易拖垮哪些环节？">4.5.1.1 面试问题：训练视频生成模型时，数据管线最容易拖垮哪些环节？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

真正容易拖垮训练的，不是某一个固定模块，而是下面四段链路中的最慢环节：

1. **读取与解码。** 远端存储带宽不足、随机 seek、编码格式混杂、损坏文件重试，都会让 CPU 侧供给出现长尾。此时最典型的现象不是报错，而是训练进程间歇性等数据。
2. **样本构造。** 镜头切分、clip 采样、过滤和 caption 决定“喂给模型的是什么”。这一段吞吐可能正常，但错误切镜、无效样本或条件错配会让有效训练信号下降。
3. **预处理与 latent 编码。** resize、裁剪和 VAE 编码若在线执行，会占用 CPU/GPU 时间；预先缓存 latent 能减轻训练时计算，却会增加存储成本，并要求 VAE 版本、归一化和数据增强策略保持一致。
4. **组 batch。** 不同帧数、分辨率和长宽比造成的 worker 负载不均与尾部等待，也会打断稳定供给。这里先关注“能否持续形成 batch”，token 利用率和 bucket 设计放到 4.5.2 再讨论。

如图 5-1 所示，排查时应给每个阶段单独记录处理耗时、队列深度、失败率和输出速率，而不是只看 GPU 利用率。

> **配图占位（图 5-1）**：展示视频训练的数据供给链路：对象存储读取 → 解码 → 镜头切分与 clip 采样 → 过滤与 caption → resize/bucket → 在线 VAE 编码或离线 latent 缓存 → batch。在线路各节点标出处理耗时、队列深度、失败率和输出速率，并突出“最慢阶段决定端到端吞吐”。

定位瓶颈时，可以做一组由简到全的替换实验：

- 用随机张量替代真实数据，先确认训练计算和通信本身的上限；
- 换成预计算 latent，判断问题是否集中在读取、解码或 VAE；
- 再依次恢复在线 VAE、视频解码和完整过滤链路，观察 samples/s、有效 tokens/s、data wait time 与 GPU 空闲时间在哪一步明显恶化。

这里有一个常见误区：**GPU 利用率低不等于数据管线一定慢**，小算子、同步通信同样可能造成空闲。只有结合阶段计时和替换实验，才能把“GPU 在等什么”定位清楚。

> **面试速答**：视频数据管线主要会卡在读取解码、样本构造、预处理或 VAE 编码、组 batch 四个环节。排查不能只看 GPU 利用率，应记录各阶段耗时和队列，再用“随机张量 → 缓存 latent → 在线 VAE → 完整视频链路”的替换实验逐层恢复，定位吞吐下降从哪一步开始。

参考资料：[Stable Video Diffusion](https://arxiv.org/abs/2311.15127)、[CogVideoX](https://arxiv.org/abs/2408.06072)

<h2 id="4.5.1.2_面试问题：视频切片、去重和镜头切分为什么会直接影响训练效果？">4.5.1.2 面试问题：视频切片、去重和镜头切分为什么会直接影响训练效果？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

切片定义了模型看到的基本时间单元，去重则改变了每类内容在训练中的实际权重。二者处理不当，即使原始视频规模很大，有效数据分布也可能已经失真。

**第一，clip 边界要符合目标任务。** 对单镜头连续生成，clip 通常应落在同一 shot 内，并尽量保留完整动作。若没有切镜条件，却把无关镜头拼进同一 clip，模型看到的只是一次无法解释的时空跳变。多镜头生成或视频编辑可以使用跨镜头样本，但切换位置和语义应被显式建模，不能与连续运动样本混在一起处理。

**第二，采样策略要控制时间分布。** 帧率、真实时长、动作是否完整以及运动强弱都会改变监督信号。大量静止 clip 会让数据分布偏向低运动，但静止样本并非都该删除：若目标产品包含静态镜头，就应保留合理比例。关键是统计并控制分布，而不是套用统一阈值。

**第三，去重要看“有效重复”，不只是文件是否相同。** 同一素材可能经过裁剪、转码、加字幕或变速后再次出现。只做文件哈希只能发现完全重复，还需结合感知特征或 clip 表征识别近重复，并按来源或模板做聚类。近重复不只是增加过拟合风险，还会悄悄放大某些主体、风格和动作的采样权重；训练集与验证集之间也要单独检查重复，避免评测偏高。

工程上应同时检查切分准确率与处理后的分布：抽样复核镜头边界的误切和漏切，统计重复簇大小、时长、帧率和运动强度，并在固定验证集上比较清洗前后的结果。这样才能判断过滤规则是在去噪，还是误删了业务需要的长尾样本。

> **面试速答**：切片决定模型学习什么样的时间变化，去重决定不同内容在训练中的实际权重。单镜头任务通常要求 clip 不跨无关切镜并尽量覆盖完整动作；采样还要控制时长、帧率和运动分布。去重则要从完全重复扩展到转码、裁剪和模板化近重复，同时防止训练集与验证集交叉污染。

参考资料：[Stable Video Diffusion](https://arxiv.org/abs/2311.15127)、[CogVideoX](https://arxiv.org/abs/2408.06072)

<h2 id="4.5.1.3_面试问题：高质量-video-caption-应该如何服务于-T2V-训练？">4.5.1.3 面试问题：高质量 video caption 应该如何服务于 T2V 训练？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

video caption 是 T2V 的条件监督。它的目标不是把文字写得尽可能长，而是用模型能够利用的表达，准确覆盖视频里确实存在的主体、动作和时间关系。

一条有效 caption 通常包含四类信息：

1. **主体与场景**：谁在什么环境中，关键属性和主体关系是什么；
2. **动作与变化**：谁做了什么，运动方向、速度或状态如何变化；
3. **时间关系**：动作是同时发生还是先后发生，事件是否分阶段推进；
4. **镜头信息**：景别、视角和相机运动，但只描述画面中能够判断的内容。

真正难的是动态信息。只根据少量关键帧生成 caption，往往能看见“有什么”，却看不准“怎么动”和“先后发生了什么”。因此工程上常把帧级视觉证据、整段视频表征、镜头边界和已有元数据合并，再生成或重写训练 caption。不同粒度的描述可以并存，但都要落回视频证据，不能为了丰富而补写不存在的动作或镜头语言。

caption 质量主要看三点：

- **事实一致**：主体、数量、属性、动作与视频相符，不能把模型猜测当成事实；
- **时间可辨**：描述能区分方向、动作阶段和先后关系，而不只是静态名词堆叠；
- **训练可用**：重要信息位于文本编码器可见范围内，表达粒度与推理时的用户 prompt 不至于严重错位。

评估时不能只用 caption 长度或单一图文相似度。更可靠的做法是构造方向、顺序、主体—动作绑定和相机运动等对照样本，检查模型能否区分近义但时序关系不同的 prompt；同时抽样做人审，尤其关注 caption 模型容易产生的动作补写和主体错配。若自动评估器与 caption 生成器同源，还要警惕二者共享错误偏好。

> **面试速答**：高质量 video caption 要基于整段视频证据，准确描述主体、动作、时间顺序和可判断的镜头变化。重点不是越长越好，而是事实正确、动态关系清楚、训练时可被文本编码器有效利用；评估时应专门测试方向、先后顺序和主体—动作绑定，不能只看图文相似度。

参考资料：[Stable Video Diffusion](https://arxiv.org/abs/2311.15127)、[CogVideoX](https://arxiv.org/abs/2408.06072)

---

<h1 id="4.5.2_Token-Budget-与训练规格：Video-VAE、帧数、分辨率和-bucketing">4.5.2 Token Budget 与训练规格：Video VAE、帧数、分辨率和 bucketing</h1>

> 视频生成训练的第一性约束是 token budget。帧数、分辨率、VAE 压缩率和 batch bucketing 本质上都在决定模型每一步需要处理多少时空 token。

<h2 id="4.5.2.1_面试问题：为什么-token-budget-是视频生成训练的核心约束？">4.5.2.1 面试问题：为什么 token budget 是视频生成训练的核心约束？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频训练成本首先由时空 token 数决定。若视频经过 VAE 压缩后的 latent 尺寸为 $T' \times H' \times W'$，则 token 数为：

```math
N=T'\times H'\times W'
```

对于 Transformer / DiT 主干，全局 attention 成本近似为：

```math
\mathcal{O}(N^2)
```

这意味着帧数、分辨率、压缩率的变化会被放大到显存和计算成本上。训练中常见的“显存突然爆”“吞吐突然下降”“某些 batch 特别慢”，很多都来自 token budget 不稳定。

工程上通常要控制的是每个 batch 的总 token：

```math
N_{\text{batch}}=\sum_{i=1}^{B}T'_iH'_iW'_i
```

一个实用原则是：**batch size 不一定固定，但 batch token budget 应尽量稳定。** 这也是多分辨率、多帧数训练必须配合 bucketing 或 dynamic batching 的原因。

面试中可以概括为：**视频训练不是按样本数算成本，而是按时空 token 数算成本。**

<h2 id="4.5.2.2_面试问题：Video-VAE-的压缩率会如何影响训练和生成质量？">4.5.2.2 面试问题：Video VAE 的压缩率会如何影响训练和生成质量？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

Video VAE 决定像素视频如何被压缩到 latent space。压缩率越高，生成主干面对的 token 越少，训练和推理越省；但压缩过强会损失纹理、身份、细小动作和时间连续性。

如果空间压缩率为 $r_h,r_w$，时间压缩率为 $r_t$，则 latent token 数近似为：

```math
N_{\text{latent}}\approx \frac{T}{r_t}\times \frac{H}{r_h}\times \frac{W}{r_w}
```

压缩率影响三类能力：

1. **视觉重建上限**：VAE 本身如果重建不出细节，生成主干很难补回来。
2. **运动保真度**：时间压缩过强可能损失快速动作和短时事件。
3. **训练效率**：latent token 少，DiT / U-Net 训练成本显著降低。

工程上需要同时看 VAE reconstruction quality 和 downstream generation quality。只看重建指标不够，因为 VAE latent 还必须适合扩散或 flow 模型学习。

面试中可以总结为：**Video VAE 是视频生成系统的计算闸门；它在压缩效率和可生成信息保留之间做核心权衡。**

<h2 id="4.5.2.3_面试问题：训练中如何设计帧数、分辨率、帧率和长宽比的-bucketing？">4.5.2.3 面试问题：训练中如何设计帧数、分辨率、帧率和长宽比的 bucketing？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频数据天然规格不统一：横屏、竖屏、不同分辨率、不同帧率、不同 clip length 都会出现。如果直接混在一个 batch 中，padding 会浪费大量 token，吞吐也会不稳定。

bucketing 的核心是把相近规格的样本放到同一 batch：

```math
\text{bucket}=(T,H,W,\text{fps},\text{aspect ratio})
```

实际设计时要考虑：

1. **控制总 token**：不同 bucket 的 batch size 可以不同，但总 token 应尽量接近。
2. **覆盖产品规格**：训练 bucket 要覆盖未来推理的横屏、竖屏、方图、短视频和长视频规格。
3. **控制运动采样**：同一帧数下，不同 fps 对应不同真实时间长度，不能混淆。
4. **避免稀疏 bucket**：过细分桶会导致数据不足和采样不均。

一个常见策略是固定 token budget，动态调整 batch size：

```math
B_k \approx \frac{N_{\max}}{T'_kH'_kW'_k}
```

其中 $B_k$ 是第 $k$ 个 bucket 的 batch size。面试中可以概括为：**bucketing 的目标不是格式整齐，而是减少 padding、稳定 token budget，并覆盖真实生成规格。**

---

<h1 id="4.5.3_训练-Recipe：图像-视频混合、课程学习与阶段化训练">4.5.3 训练 Recipe：图像-视频混合、课程学习与阶段化训练</h1>

> 视频生成模型通常不是一步训成的。更常见的 recipe 是：图像预训练建立空间先验，视频预训练建立时间先验，高质量视频微调提升可用性，后训练进一步优化指令遵循和偏好。

<h2 id="4.5.3.1_面试问题：图像-视频联合训练的混合比例应该如何理解？">4.5.3.1 面试问题：图像-视频联合训练的混合比例应该如何理解？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

图像-视频联合训练不是简单“数据越多越好”，而是在空间质量和时间能力之间做权衡。图像数据帮助模型学习物体、构图、纹理、风格和文本-图像语义；视频数据帮助模型学习运动、时序一致性和镜头变化。

混合训练目标可写成：

```math
\mathcal{L}=\lambda_{\text{img}}\mathcal{L}_{\text{img}}+\lambda_{\text{vid}}\mathcal{L}_{\text{vid}}
```

关键不只是 $\lambda_{\text{img}}$ 和 $\lambda_{\text{vid}}$ 的数值，还包括采样频率、token 数、batch 组织和训练阶段。图像样本 token 少、质量高，过多会让模型偏静态；视频样本 token 多、噪声大，过多可能拖慢收敛并降低单帧质量。

工程上常见策略是前期提高图像比例稳定空间生成，中后期提高视频比例强化运动和一致性。面试中可以概括为：**图像补空间，视频补时间；混合比例决定模型更偏画质、运动还是综合能力。**

<h2 id="4.5.3.2_面试问题：为什么视频生成训练通常需要-progressive-training？">4.5.3.2 面试问题：为什么视频生成训练通常需要 progressive training？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

progressive training 的核心是逐步增加任务难度，避免一开始就在高分辨率、长序列、多条件、强噪声数据上训练导致不稳定。

常见阶段包括：

```text
image pretraining
-> low-res short-video pretraining
-> higher-res / longer-clip training
-> high-quality video finetuning
-> instruction / preference post-training
```

每个阶段解决的问题不同：图像阶段建立空间先验，短视频阶段学习局部运动，高分辨率阶段补视觉细节，长视频阶段增强状态保持，高质量微调阶段提升审美和可用性。

如果直接训练最终规格，常见问题是 loss 抖动大、运动学不稳、显存成本过高、数据噪声放大。progressive training 可以降低优化难度，并让模型逐步适应更高 token budget。

面试中可以总结为：**视频生成训练通常是分阶段构建能力，而不是一次性把所有难题同时交给模型。**

<h2 id="4.5.3.3_面试问题：高质量微调和偏好后训练主要解决什么问题？">4.5.3.3 面试问题：高质量微调和偏好后训练主要解决什么问题？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

大规模预训练解决“模型是否具备基础分布能力”，高质量微调和偏好后训练解决“模型是否可用、好用、符合用户偏好”。

高质量微调通常使用更干净、更高分辨率、更准确 caption 的精选数据，主要改善：

1. 单帧视觉质量和审美。
2. 动作自然度和运动幅度。
3. 文本遵循和镜头语言。
4. 人物、手部、文字、复杂物体等难点区域。

偏好后训练则进一步对齐人类偏好，例如减少伪影、增强主体一致性、提升 prompt following、改善构图和镜头节奏。它可以采用人工偏好、VLM 评估、奖励模型或 DPO/RL 类方法。

面试中可以概括为：**预训练决定能力边界，精选微调提升质量分布，偏好后训练让模型更接近真实产品需求。**

---

<h1 id="4.5.4_训练稳定性：噪声调度、条件-dropout、loss-weighting-与异常排查">4.5.4 训练稳定性：噪声调度、条件 dropout、loss weighting 与异常排查</h1>

> 视频训练中的“不稳定”很少只有一个原因。它可能来自数据噪声、caption 错误、token budget 变化、VAE latent 分布、噪声调度、学习率、混合精度或分布式通信。

<h2 id="4.5.4.1_面试问题：视频扩散或-Flow-Matching-训练中常见不稳定现象有哪些？">4.5.4.1 面试问题：视频扩散或 Flow Matching 训练中常见不稳定现象有哪些？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频生成训练的不稳定通常先表现在样本质量上，而不一定只体现在 loss 上。常见现象包括：

1. **loss 正常但样本差**：模型可能学到平均化结果，运动弱或细节糊。
2. **单帧质量好但视频闪烁**：空间先验强，时间建模不足。
3. **prompt 跟随差**：caption 噪声、CFG dropout 不合理或文本编码弱。
4. **运动塌缩**：训练数据过静、motion bucket 失衡或条件过强。
5. **高频伪影**：VAE 重建不足、低码率数据污染或 loss 权重不合理。
6. **训练中后期退化**：学习率、EMA、数据阶段切换或混合比例变化不当。

工程排查时，不能只看整体 loss。更实用的是固定一组 validation prompts，周期性生成样本，观察画质、运动、时序一致性、文本遵循和失败类型。

面试中可以总结为：**视频训练稳定性要看 loss、样本、数据分布和系统吞吐的联合信号；loss 下降不等于视频生成质量提升。**

<h2 id="4.5.4.2_面试问题：Diffusion-与-Flow-Matching-在工程训练上各有什么关注点？">4.5.4.2 面试问题：Diffusion 与 Flow Matching 在工程训练上各有什么关注点？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

Diffusion 通常学习去噪预测，典型目标为：

```math
\mathcal{L}_{\text{diff}}=
\mathbb{E}_{x_0,\epsilon,t}
\left[
\lVert \epsilon-\epsilon_\theta(x_t,t,c)\rVert^2
\right]
```

Flow Matching 学习从噪声到数据的连续速度场，可抽象为：

```math
\mathcal{L}_{\text{fm}}=
\mathbb{E}_{x_t,t}
\left[
\lVert v_\theta(x_t,t,c)-u_t(x_t)\rVert^2
\right]
```

工程上，Diffusion 更关注 noise schedule、prediction type、SNR weighting、sampler 和 CFG 配合；Flow Matching 更关注路径设计、时间采样、速度场尺度、ODE/SDE sampler 以及少步采样稳定性。

两者在视频场景下共同面对的问题是：高维时空 token、数据噪声、运动建模和长序列成本。不能简单说 Flow Matching 一定更好，或 Diffusion 一定过时。更严谨的说法是：**Diffusion 和 Flow Matching 是不同训练目标与采样路径设计，工程优劣取决于模型结构、数据规模、采样器和部署约束。**

<h2 id="4.5.4.3_面试问题：CFG-dropout、EMA、timestep-sampling-和-loss-weighting-分别解决什么问题？">4.5.4.3 面试问题：CFG dropout、EMA、timestep sampling 和 loss weighting 分别解决什么问题？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

这些机制分别控制条件能力、参数稳定性、训练时间步分布和不同噪声区间的优化平衡。

**CFG dropout** 让模型在训练中同时学习有条件和无条件预测，推理时可以做 classifier-free guidance：

```math
\hat{\epsilon}
=\epsilon_\theta(x_t,t,\varnothing)
+s\left(\epsilon_\theta(x_t,t,c)-\epsilon_\theta(x_t,t,\varnothing)\right)
```

dropout 太低，无条件分支学不好；dropout 太高，条件跟随可能变弱。

**EMA** 用滑动平均降低参数震荡：

```math
\theta_{\text{ema}}\leftarrow \beta\theta_{\text{ema}}+(1-\beta)\theta
```

**timestep sampling** 决定模型更频繁学习哪些噪声阶段。不同时间步对应不同难度：高噪声阶段更偏全局语义，低噪声阶段更偏细节和纹理。

**loss weighting** 用于平衡不同 SNR、不同区域、不同帧或不同任务的梯度贡献，避免训练被某一类样本或某一噪声区间主导。

面试中可以概括为：**CFG dropout 控条件，EMA 稳参数，timestep sampling 控训练分布，loss weighting 控梯度贡献。**

---

<h1 id="4.5.5_分布式训练效率：数据吞吐、显存优化与并行策略">4.5.5 分布式训练效率：数据吞吐、显存优化与并行策略</h1>

> 视频训练的效率瓶颈通常是系统级的。只讨论模型 FLOPs 不够，还要看数据读取、VAE 编码、token padding、activation memory、通信开销和 GPU 利用率。

<h2 id="4.5.5.1_面试问题：视频训练吞吐低，应该从哪些环节排查？">4.5.5.1 面试问题：视频训练吞吐低，应该从哪些环节排查？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频训练吞吐低时，不应直接归因于模型太大。排查应按链路进行：

```text
storage -> video decode -> sampling/augment -> VAE encode/cache
        -> batch bucketing -> GPU forward/backward -> communication
```

常见瓶颈包括：

1. **存储与网络 IO**：远程读取视频导致 dataloader 等待。
2. **CPU 解码**：视频随机 seek 和解码速度跟不上 GPU。
3. **在线 VAE 编码**：VAE encode 成为训练前置瓶颈。
4. **padding 浪费**：bucket 设计差，实际有效 token 占比低。
5. **通信开销**：多机训练中梯度同步或参数切分通信过重。
6. **负载不均衡**：不同 batch token 数差异大，step time 抖动。

工程上常用指标包括：samples/sec、tokens/sec、GPU utilization、dataloader wait time、step time p50/p95、padding ratio。对于视频训练，**effective tokens/sec** 往往比 samples/sec 更有解释力。

面试中可以总结为：**视频训练吞吐优化要按端到端链路排查，很多时候瓶颈在数据和 batch 组织，而不在模型计算本身。**

<h2 id="4.5.5.2_面试问题：视频模型训练中如何做显存优化，而不是简单减-batch？">4.5.5.2 面试问题：视频模型训练中如何做显存优化，而不是简单减 batch？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

简单减 batch 会降低吞吐和训练稳定性，不是最优解。视频模型显存主要来自参数、优化器状态、梯度、激活、attention 中间量和输入 latent。

常见优化方式包括：

1. **activation checkpointing**：用重算换显存。
2. **mixed precision / FP8**：降低参数和激活显存。
3. **memory-efficient attention**：减少 attention 中间矩阵占用。
4. **FSDP / ZeRO**：切分参数、梯度和优化器状态。
5. **sequence parallel**：沿 token 维度拆分长序列。
6. **bucket token budget**：避免某些 batch 因 token 过多 OOM。
7. **VAE latent precompute**：减少训练时 VAE activation 占用。

需要注意，显存优化往往改变计算和通信成本。例如 checkpointing 会降低吞吐，FSDP 会增加通信，sequence parallel 对实现要求更高。

面试中可以概括为：**显存优化不是简单缩小 batch，而是在激活、参数、序列长度、通信和重算之间做系统权衡。**

<h2 id="4.5.5.3_面试问题：数据并行、张量并行、序列并行和-FSDP-如何组合？">4.5.5.3 面试问题：数据并行、张量并行、序列并行和 FSDP 如何组合？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

不同并行方式解决不同瓶颈：

| 并行方式 | 拆分对象 | 主要解决 |
|---|---|---|
| Data Parallel | batch | 数据规模和吞吐 |
| Tensor Parallel | 层内矩阵 | 单层计算和参数过大 |
| Sequence Parallel | token 序列 | 长视频上下文过长 |
| FSDP / ZeRO | 参数、梯度、优化器状态 | 模型状态显存 |
| Pipeline Parallel | 模型层 | 超深模型跨设备放置 |

视频 DiT 训练中，瓶颈经常来自两个方向：模型参数大和时空 token 长。只用 data parallel 不能解决单卡显存；只用 FSDP 也不能完全解决长序列 attention。实际训练常需要组合：

```text
data parallel + FSDP + sequence parallel + activation checkpointing
```

若模型单层非常大，还可能加入 tensor parallel。选择并行策略时要看瓶颈是 parameter memory、activation memory、attention memory 还是 communication。

面试中可以总结为：**视频模型分布式训练不是套一个并行策略，而是根据参数规模和时空 token 规模组合多种并行方式。**

---

<h1 id="4.5.6_推理部署优化：采样加速、VAE-解码、服务调度与成本控制">4.5.6 推理部署优化：采样加速、VAE 解码、服务调度与成本控制</h1>

> 视频模型能训练出来，不代表能在线服务。线上推理要同时优化采样步数、VAE 解码、显存峰值、队列调度、失败重试和单位生成成本。

<h2 id="4.5.6.1_面试问题：视频生成线上推理为什么常常慢在不止一个地方？">4.5.6.1 面试问题：视频生成线上推理为什么常常慢在不止一个地方？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

视频推理延迟来自整条链路，而不只是去噪主干：

```text
prompt processing -> text / image encoding -> denoising steps
-> VAE decode -> post-processing -> safety check -> video encoding
```

粗略成本可写成：

```math
C_{\text{infer}}\approx
S\cdot C_{\text{denoise}}(T,H,W)
+C_{\text{vae-decode}}
+C_{\text{condition}}
+C_{\text{post}}
```

其中 $S$ 是采样步数。对于高分辨率或长视频，VAE decode、视频编码、上传下载和安全审核也可能成为显著瓶颈。

工程排查时需要分别记录：首帧时间、总生成时间、去噪时间、VAE 解码时间、排队时间、显存峰值和失败率。只优化主干模型，不一定能显著降低端到端延迟。

面试中可以概括为：**视频生成慢是端到端链路慢，不能只看 diffusion step 的耗时。**

<h2 id="4.5.6.2_面试问题：视频生成推理加速有哪些工程路线？各自代价是什么？">4.5.6.2 面试问题：视频生成推理加速有哪些工程路线？各自代价是什么？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

常见推理加速路线包括：

| 路线 | 作用 | 代价 |
|---|---|---|
| 减少采样步数 | 降低 denoising 时间 | 细节和稳定性可能下降 |
| 蒸馏 / consistency | 少步生成 | 多样性和可控性可能下降 |
| 低精度推理 | 降低显存和计算 | 数值稳定性风险 |
| KV / condition cache | 复用条件特征 | 适用场景有限 |
| VAE tiling / slicing | 降低解码显存 | 可能引入边界伪影 |
| dynamic batching | 提升吞吐 | 增加排队延迟 |
| 多档模型 | 区分预览和高质量生成 | 系统复杂度增加 |

实际部署中，加速通常不是单点方案，而是组合：少步采样 + 半精度 + VAE 优化 + 动态 batch + 队列调度。

面试中应强调代价：**推理加速一定要同时评估质量、稳定性、显存、吞吐和失败率。**

<h2 id="4.5.6.3_面试问题：工业部署中如何设计质量、延迟、吞吐和成本的分层策略？">4.5.6.3 面试问题：工业部署中如何设计质量、延迟、吞吐和成本的分层策略？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

工业部署不是追求每次生成都用最高质量配置，而是在不同场景下提供不同质量和成本档位。可以抽象为：

```math
\max Q_{\text{video}}
\quad \text{s.t.}\quad
L\leq L_{\max},\;
M\leq M_{\max},\;
C\leq C_{\max}
```

常见分层策略包括：

1. **预览模式**：低分辨率、短帧数、少步采样，用于快速反馈。
2. **标准模式**：平衡质量和速度，作为默认线上配置。
3. **高质量模式**：更多采样步数、更高分辨率、更强后处理，适合离线任务。
4. **异步队列**：长视频和高质量任务进入后台队列，避免阻塞交互。
5. **失败回退**：OOM、超时或安全失败时自动降级规格或重试。

还需要按业务监控单位生成成本，例如每秒视频成本、GPU 分钟成本、失败重试成本和队列等待时间。

面试中可以总结为：**视频生成部署的核心不是单次效果最强，而是在可控成本下稳定提供分层质量服务。**

---

<h1 id="4.5.7_视频生成评估体系：多维质量、人类偏好与工程闭环">4.5.7 视频生成评估体系：多维质量、人类偏好与工程闭环</h1>

> 评估不是模型训练结束后补一个分数，而是连接数据、训练、推理、发布与用户反馈的质量反馈系统。一个能长期使用的体系不应绑定某个模型、某个 judge 或某张榜单，而应持续回答三个问题：视频是否可靠、是否遵循条件、是否真的让用户满意。

<h2 id="4.5.7.1_面试问题：为什么视频生成不能依赖单一指标判断模型好坏？">4.5.7.1 面试问题：为什么视频生成不能依赖单一指标判断模型好坏？</h2>

**难度评分：⭐⭐⭐⭐ (4/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

因为“视频好”并不是单一能力。一个模型可能画面清晰，却不按 prompt 生成；可能相邻帧流畅，却在远距离镜头中换了人物；也可能自动分数更高，但动作违背常识、用户仍然不愿意使用。

常见单项指标各自只观察一个投影：

| 指标/方法类型 | 擅长回答 | 不能单独保证 |
|---|---|---|
| 分布距离，如 FVD | 生成结果与真实视频分布是否接近 | 指令是否完成、某个样本是否可用 |
| 图文/文视频相似度 | 语义大致是否相关 | 关系、动作顺序和局部细节是否正确 |
| 帧级画质或美学分数 | 单帧是否清晰、自然 | 跨帧稳定、镜头和事件是否连续 |
| 光流、轨迹或时序分数 | 运动是否平滑、是否有闪烁 | 运动是否符合 prompt 和物理常识 |
| 人工偏好 | 用户整体是否喜欢 | 是否覆盖全部风险场景、能否低成本高频运行 |

工程上最危险的情况是把一个 proxy 当成最终目标：例如只优化画质分数，模型可能趋向低运动的“漂亮静帧”；只优化文本相似度，可能得到语义词汇齐全但动作不自然的视频。指标被直接作为优化目标后，还可能被模型“钻空子”。

因此，正确做法不是寻找万能分数，而是用**多维 scorecard + 分桶诊断 + 人类校准**共同判断。FVD 提供了分布级评估的起点；VBench、EvalCrafter 等工作进一步说明，质量需要拆成可解释维度，并验证其与人类判断的一致性。

面试中可以总结为：**单指标只能反映一种代理信号；视频评估要用多维画像判断模型到底在哪些能力上变好、又在哪些能力上退化。**

<h2 id="4.5.7.2_面试问题：一个视频生成评估体系应包含哪些核心维度？">4.5.7.2 面试问题：一个视频生成评估体系应包含哪些核心维度？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

应围绕用户感知与视频本体能力，而不是围绕某一代模型的架构名称。无论底层是 U-Net、DiT、Flow Matching 还是未来的统一模型，以下六类问题始终成立：

| 维度 | 核心问题 | 典型失败现象 |
|---|---|---|
| 视觉质量 | 单帧是否清晰、自然、可辨识 | 模糊、伪影、畸形、低美学质量 |
| 时序与运动 | 内容和运动是否连续、稳定、自然 | flicker、抖动、动作冻结、速度异常 |
| 条件遵循 | 是否完成文本、参考图、首尾帧、结构控制等要求 | 漏对象、属性错配、动作/关系错误 |
| 一致性与叙事 | 主体、背景、状态和事件是否能持续保持 | 换脸、换装、场景漂移、事件断裂 |
| 物理与几何可信度 | 交互、遮挡、空间和因果是否合理 | 穿模、物体凭空变化、违反重力或碰撞规律 |
| 用户偏好与安全 | 是否符合审美、可编辑性和业务规范 | 虽“正确”但不可用，或存在安全风险 |

不同任务只是在这六类能力上的权重不同：T2V 更重视条件遵循，I2V 更重视参考主体与首帧保持，长视频更重视状态记忆、跨镜头一致性和叙事，音视频任务还需加入音画同步。这里的长视频专项评估可进一步参考第 4.4.6 节，而不是在本节重复展开。

实践中，测试集也必须按能力分桶，而不是只保存一个平均分。例如至少维护：静态/高运动、单主体/多主体、简单/组合指令、短视频/长视频、真实物理交互/普通场景、常见/高风险业务 prompt。只有这样，平均分提升才可以被解释为真实能力提升。

面试中可以总结为：**长期有效的评估框架评估的是视觉、时间、语义、一致性、世界可信度和用户价值；具体指标可以更新，能力维度不能缺席。**

<h2 id="4.5.7.3_面试问题：自动指标、人类评测和多模态大模型评审应如何分工？">4.5.7.3 面试问题：自动指标、人类评测和多模态大模型评审应如何分工？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

三者不是替代关系，而是成本、覆盖率与可信度不同的三层机制：

```text
自动指标：高频、低成本、适合回归监控
    -> 人工评测：低频、高可信、定义“用户真正认为好”
        -> 多模态大模型评审：规模化解释与筛查，但需持续校准
```

1. **自动指标用于守门和定位。** 每次训练、每个 checkpoint、每次代码改动都应运行固定集，快速发现画质、闪烁、文本相似度或延迟的明显回退。它们适合趋势监控，不应单独决定模型胜负。
2. **人工评测用于建立金标准。** 对代表性样本做盲测、成对偏好或分维打分，尤其覆盖高价值、高风险和自动指标分歧大的样本。要记录标注规范、一致性和置信区间，避免把偶然偏好当作结论。
3. **多模态大模型评审用于扩展人评。** 它可按检查表评估对象、动作、关系和时间变化，也能生成失败原因；但 judge 本身有模型偏好、提示词敏感和能力盲区，不能未经校准直接替代人工。

实战中的可靠流程是：先用人工集计算自动指标或 judge 与人类偏好的相关性；对相关性较低的维度降权或停用；版本升级后重新做校准。对于“人物是否换脸”“文字是否可读”“物理交互是否成立”这类高风险能力，应保留专门人工抽检，而不能只相信总分。

面试中可以概括为：**自动指标负责高频回归，人评负责定义真实质量，MLLM judge 负责规模化辅助；所有自动评估都必须以人类偏好校准。**

<h2 id="4.5.7.4_面试问题：模型离线分数提升但用户体验没有变好，应如何排查？">4.5.7.4 面试问题：模型离线分数提升但用户体验没有变好，应如何排查？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐ (4/5)**

先不要假设用户错了或指标错了，而要把问题视为**离线评估分布、代理指标和线上业务目标之间发生了错配**。典型排查顺序如下：

1. **检查评测集覆盖。** 离线 prompt 是否过于简单、重复或偏向训练集？线上是否更多是长 prompt、多主体、参考图、人物、文字、镜头控制或长视频？先按业务场景分桶重算，而不是只看总平均分。
2. **检查指标是否奖励了错误行为。** 例如画质分数提高但运动变弱，平均时序分数提高但模型生成趋于保守，文本相似度提高但关键关系仍错误。应同时查看样本、失败标签和各维度变化。
3. **检查生成配置是否一致。** 离线与线上若使用不同 prompt 重写、采样步数、CFG、seed 策略、分辨率、后处理或安全过滤，比较就不再公平。
4. **检查端到端体验。** 用户感知的不只是视频质量，还包括首帧等待、失败率、重试次数、可控性、编辑成功率和成本。一个更慢、更不稳定的模型，即使离线质量更高，也可能带来更差体验。
5. **回到人类偏好验证。** 对线上争议样本做盲测或 A/B 偏好评估，确认是离线指标失效、业务权重设置不当，还是用户群体偏好发生变化。

一个常用的工程诊断表是：

```text
总分上升
├─ 分桶是否均升？            否 -> 定位退化场景
├─ 人类偏好是否同步上升？    否 -> 重新校准指标/权重
├─ 线上配置是否一致？        否 -> 修正实验设计
└─ 延迟、失败率、可控性变差？是 -> 按端到端目标重新决策
```

面试中可以总结为：**离线分数不是用户体验本身；先做场景分桶和端到端归因，再用人类偏好决定是否真的上线。**

<h2 id="4.5.7.5_面试问题：如何把视频评估接入训练、发布和线上质量优化闭环？">4.5.7.5 面试问题：如何把视频评估接入训练、发布和线上质量优化闭环？</h2>

**难度评分：⭐⭐⭐⭐⭐ (5/5)  |  考察频率：⭐⭐⭐⭐⭐ (5/5)**

成熟团队不会在模型训练结束后才开始评估，而是让评估贯穿整个生命周期：

```text
失败样本/业务需求
-> 固化为带标签的评测集与能力分桶
-> checkpoint 自动回归
-> 人工/MLLM 抽检与校准
-> 候选模型对照实验
-> 灰度发布与线上监控
-> 新失败样本回流数据、评测集和训练
```

工程落地时可维护三套互补集合：

| 集合 | 用途 | 更新原则 |
|---|---|---|
| 固定回归集 | 保证核心能力不回退 | 版本稳定、不可随意替换 |
| 挑战集 | 暴露组合指令、运动、物理、长时一致性等短板 | 随失败模式持续扩充 |
| 线上代表集 | 贴近真实业务分布与用户价值 | 定期重采样并做隐私、安全处理 |

发布门槛也不能只写成“总分大于某值”，而应包含：核心场景不得回退、关键安全/一致性问题不得恶化、端到端延迟和失败率在预算内、人工偏好达到最小提升阈值。若某次模型只改善小众美学场景，却损害人物一致性或线上成功率，就不应直接全量替换。

最终，评估产物应能直接驱动行动：把失败视频归因到数据问题、条件解析问题、时空建模问题、采样/解码问题或服务配置问题，并为每类问题指定负责人和验证集。这样评估才不是报表，而是模型迭代的控制面。

面试中可以概括为：**把评估当成持续演化的质量基础设施：固定集守回归、挑战集找上限、线上集对齐用户，失败样本再反哺数据和训练。**
